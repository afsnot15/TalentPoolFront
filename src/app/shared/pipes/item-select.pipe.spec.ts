import { ItemSelectPipe } from './item-select.pipe';

describe('ItemSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
